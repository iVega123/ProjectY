# Merge with the shared application before inclusion.
services:
  api-gateway:
    build:
      target: final
    restart: unless-stopped
    healthcheck:
      test: [CMD, /app/api-gateway, --healthcheck]
  auth-gate:
    restart: unless-stopped
  rider-manager:
    restart: unless-stopped
  moto-hub:
    restart: unless-stopped
  rental-operations:
    restart: unless-stopped
