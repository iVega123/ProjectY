apiVersion: v1
kind: Service
metadata:
  name: kafka
  namespace: projecty
spec:
  $patch: replace
  type: ExternalName
  externalName: projecty-kafka-kafka-bootstrap.projecty.svc.cluster.local
  ports:
    - name: broker
      port: 9092
      targetPort: 9092
